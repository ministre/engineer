version: "3.7"
   
services:
  web:
    build: ./app
    command: python engineer_django/manage.py runserver 0.0.0.0:8000
    volumes:
      - ./app/:/opt/app/
    ports:
      - "8000:8000"
    env_file:
      - ./.env.dev
